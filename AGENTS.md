# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

## Git 提交规范

每次代码修改完成后，**必须**执行 git commit 提交变更，commit message 使用中文撰写，格式参考：

```
<type>: <简短描述>

- <变更点1>
- <变更点2>
```

type 使用：feat / fix / refactor / style / chore 等。
